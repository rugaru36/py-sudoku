class Matrix_Output_Helper:
    def output_matrix_with_hidden_elements(num_matrix, bool_matrix, selected_row = None, selected_col = None):
        horizontal_separator = '\n'
        for row in range(num_matrix):
            if row == selected_row:
                horizontal_separator = 
                    '\n' if selected_row == None 
                    else selected_row == None '_' * (len(num_matrix[0]) + 2)
                    
            print(horizontal_separator)
            row_with_nums = ' '
            for col in range(num_matrix[row]):
                row_with_nums += '' 
                
                
        print()